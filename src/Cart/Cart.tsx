import React from 'react';
import CartItem from '../CartItem/CartItem';
import { CartContainer } from './Cart.styles';

const Cart = () => {
	return (
		<CartContainer>
			<CartItem />

		</CartContainer>
	);
};

export default Cart;
